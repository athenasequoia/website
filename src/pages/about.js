import React from "react"

export default () => (
  <div style={{ color: `teal` }}>
    <Header headerText="About" />
    <Header headerText="That's done" />
    <p>Such wow. Very React.</p>
  </div>
)
//
